<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ycble.runchinaup">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />


    <uses-permission android:name="android.permission.READ_PHONE_STATE" />



    <application>

        <service
            android:name=".aider.NPNotificationService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name=".aider.NPAccessibilityService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
        </service>
        <!--<service-->
            <!--android:name=".ota.absimpl.nordic.DfuService"-->
            <!--android:exported="false"-->
            <!--android:enabled="true"-->
            <!--android:label="DfuService">-->
            <!--<intent-filter>-->
                <!--<action android:name="no.nordicsemi.android.action.DFU_UPLOAD" />-->

                <!--<category android:name="android.intent.category.DEFAULT" />-->
            <!--</intent-filter>-->
        <!--</service>-->

        <!--<activity android:name=".ota.absimpl.nordic.NotificationActivity" />-->
    </application>
</manifest>
